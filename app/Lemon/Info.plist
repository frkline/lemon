<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Hide from Dock — menu bar only -->
	<key>LSUIElement</key>
	<true/>
	<key>NSAppleEventsUsageDescription</key>
	<string>Lemon uses Terminal to launch Claude sessions.</string>
	<!--
	  The "Local Network" prompt this triggers is a false alarm: Lemon does no LAN
	  discovery. The on-device Gemma classifier (SwiftLM) binds 127.0.0.1 only, and
	  Lemon talks to it over pure loopback — the Mac talking to itself. macOS exempts
	  loopback from the restriction, so classification works whether the user allows
	  or denies. This honest copy replaces macOS's generic "collect data from devices"
	  default; it does not suppress the prompt.
	-->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Lemon uses a local (loopback) connection to talk to its on-device Gemma classifier. Nothing is sent to other devices on your network.</string>
	<key>NSHumanReadableCopyright</key>
	<string></string>
</dict>
</plist>
