{
  "name": "lemon",
  "version": "0.1.0",
  "description": "Mac-native AI developer agent — tag a Linear or GitHub issue, get a PR",
  "private": true,
  "scripts": {
    "test:scripts": "node scripts/get-field.js config.example.yaml model"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
