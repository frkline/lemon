## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] Clean build with warnings-as-errors (`make build-ui` or the `xcodebuild ... build` command)
- [ ] Tests pass (`make test`)
- [ ] No hardcoded colors/animations in views — used `LD.*` design tokens
- [ ] No secrets committed (secrets belong in Keychain only)
- [ ] For UI changes: before/after smoke screenshots attached below

## Screenshots

<!-- For UI changes, drop before/after images from `make ui` here. -->
